[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    unit: Pruebas unitarias (sin GUI)
    functional: Pruebas funcionales (con GUI)
    integration: Pruebas de integración
